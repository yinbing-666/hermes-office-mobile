# UI-REFINE-ROUND2-BRIEF — 细节打磨（第二轮）

## 背景
项目：/home/agentuser/projects/hermes-office-mobile（office.icewill.tech）
上一轮已完成：全局设计 token 升级（品牌色 #2f6f9f、字号/间距/圆角阶梯）、顶部栏收敛、tab 图标重绘（24x24 线性 SVG）、3D 插画卡片分层。本轮只修细节，目标是消除「粗糙感」。

## 你的权限
- ✅ 改 `frontend/src/styles.css`、`frontend/src/App.tsx`、`frontend/src/components/OfficeIcon.tsx`
- 🚫 禁止动后端、禁止删 tab/模块、禁止加依赖、禁止改功能逻辑

## 要修的细节（截图自检发现）

### 首页
1. **占位符「—」**：在线员工/离线员工/今日 API 调用无数据时显示「—」，像没做完。改为：数据为 0 显示「0」（灰色），加载中保持「—」但配灰色弱化
2. **0 值颜色**：待处理任务「0」是深色，与旁边的绿色数字（3950）不统一。规则：有数据用正常色，0 用灰色
3. **tab 激活态**：当前激活态是填充图标+浅背景，对比度略低。改为「底部指示条」（active tab 下加 2-3px 圆角指示条，品牌色），更现代
4. **复选框原生样式**：虚拟办公区的「茶水/健身/工位」复选框是原生 HTML 样式，与整体脱节。自定义 checkbox：圆角 4px、品牌色边框、选中填充品牌色 + 白色对勾（用 CSS 实现，不加依赖）
5. **TODAY 区块空**：「今日待处理」只有一行文字显得塌陷。加空状态微文案（如「当前没有待处理任务，休息一下」）配灰色图标（复用 OfficeIcon 图标，不用 emoji）
6. **品牌字号**：顶部栏「HERMES OFFICE」字号偏小，与页面标题（办公室 21px）拉不开差距。品牌用 10px/650 + letter-spacing .18em，页面标题保持 21px/650，拉开层次

### 成本页
7. **图表标签拥挤**：14 天趋势图上方数值标签（816K/208.3M/102.6M）间距过密可能重叠。改为：仅显示最大值/最小值标签，或字号降到 9px + 只显示 >50M 的标签
8. **卡片高度不一致**：「今日成本估算」因文字换行比其他三格高。缩短文案为「今日成本」（或调整 padding 让四格等高）
9. **堆叠图颜色同质化**：输入/输出/缓存三段颜色过于接近（都是蓝色系）。改为三段对比色：输入=品牌蓝、输出=绿色、缓存=浅灰，保持克制饱和度
10. **图例位置**：「含缓存命中」图例悬浮在右侧空白处，视觉 disconnected。移到图表正下方居中
11. **模型列表空状态**：按模型拆分只显示一个模型时显得空。加提示「暂无其他模型数据」

## 硬约束
- ❌ 不用 emoji、不用重阴影（alpha≤.08）、不用玻璃拟态、不加渐变堆砌、不加花哨动画
- ✅ 保持浅色办公风（米灰/白底）、1px 边框、克制圆角
- ✅ 所有改动纯 CSS + 少量 TSX 类名调整，不新增依赖

## 验证
```bash
cd /home/agentuser/projects/hermes-office-mobile/frontend && npm run build
```
必须通过。不要删任何现有 class 名（除非确认无引用），不要动后端。
