# Hermes Office 视觉审计任务书（CPA API 直接分析版）

你是高级 UI/UX 审计师。请**只读分析** `/home/agentuser/projects/hermes-office-mobile`，不修改任何文件，输出结构化审计报告。

## 项目设计规范（必须对照）

Marvis Office 浅色办公风：
- 背景：米灰/白（约 #f6f7f5 系）
- 卡片：白底 + 1px 边框（#e5e7e5 系）+ 克制圆角
- 文字：主文字深灰蓝（约 #2c3a4a / #334155 系）、次要文字浅灰（#64748b 系）
- 图标：线性、单色，与文字同色系
- **禁止**：emoji、重阴影、玻璃拟态、荧光色、彩虹渐变、深色块配浅色文字但对比不足

## 用户痛点（重点审计方向）

「有些页面明显就是像是一些文本和背景一点都不符」——文字和背景不协调。请特别找：
1. 深色/彩色背景块上的文字颜色（对比度不足或违和）
2. 卡片/区块头背景色与文字色的搭配问题
3. 不同页面之间风格跳跃（某个页面颜色体系跟全局不一致）
4. 全局 CSS 变量（--bg / --surface / --text / --muted 等）是否统一被使用，还是大量硬编码颜色导致不一致

## 审计范围

- `frontend/src/styles.css`（重点，所有页面共用的样式）
- `frontend/src/App.tsx`（各页面 JSX 里的内联样式 / className）
- `frontend/src/components/` 下所有组件

## 输出格式（中文，结构化）

```
## 审计报告

### 1. 全局色板问题
- [Critical/High/Medium] 描述（文件:行号）

### 2. 各页面文本 vs 背景问题
按页面分组：Office / Evolution / Analytics / Cost / Sessions / Login / Workflow
每条：具体位置、问题、建议修复值（给出具体色值）

### 3. 风格一致性
- 哪些地方偏离了 Marvis 浅色规范
- 是否有页面用了完全不相干的颜色

### 4. 修复优先级排序
按「用户能感知的违和感」排序，标出哪些最该先修
```

## 约束

- 只读，不修改任何文件
- 引用具体文件:行号，不要泛泛而谈
- 每条问题给出建议的具体修复色值（如 `color: #2c3a4a` → `#64748b`）
