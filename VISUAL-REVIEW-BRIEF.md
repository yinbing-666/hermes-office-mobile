# Hermes Office 全站视觉 Review + 修复任务书

你是高级产品工程师和前端架构师。请 review 并**直接修复** `/home/agentuser/projects/hermes-office-mobile` 的视觉质量问题。

## 用户痛点（原话）

「其他页面也都不太行，有些页面明显就是像是一些文本和背景一点都不符」

即：多个页面的**文字颜色和背景色对比度/协调性有问题**——看起来「文本和背景不符」，像是文字悬浮在不搭的背景上。需要你找出所有这类问题并修复。

## 项目背景

- 移动端优先 PWA「Hermes Office / AI 员工工作台」，多 Agent（小黑/小橙/小金）移动办公入口
- 设计风格：**Marvis Office 浅色办公风**——米灰/白底、1px 边框、克制圆角、线性图标、无 emoji、无重阴影、无玻璃拟态
- 用户极度反感：AI 生成感、花哨渐变、暗色块配亮文字不协调、卡片风格不统一

## 文件范围

- `frontend/src/App.tsx`（主页面：Office / Evolution / Analytics / Cost / Sessions 等）
- `frontend/src/styles.css`（全局样式）
- `frontend/src/components/`（evolution、topics 等子组件）
- `frontend/src/types.ts` / `api.ts`（如需要）

## 重点检查项（按优先级）

### 1. 文本 vs 背景协调性（用户核心痛点）
逐个页面检查：
- 深色/彩色背景上是否用了不合适的文字色（如深色文字压在深色块上、白字压在浅色块上）
- 标题块/卡片头的背景色和文字色对比度是否足够
- 是否有「文字悬浮、背景突兀」的区块（比如某个 section 背景色跟全局米灰不一致，或者某个标题颜色跟所在卡片不搭）
- 表格/列表的行 hover、选中态文字颜色是否正确

### 2. 全局风格一致性
- 所有页面是否统一：米灰/白底（约 #f6f7f5 系）、1px 边框（#e5e7e5 系）、克制圆角
- 是否有页面/组件用了不相干的颜色（荧光色、过深背景、彩虹渐变）
- 卡片、按钮、标签的样式是否统一

### 3. 移动端适配
- 小屏下是否有文字溢出、卡片过宽、间距失调
- 检查 @media 断点下的样式

## 修复要求

- **直接改代码**（不是只给建议），改完 build 验证
- 风格铁律：保持 Marvis Office 浅色风，**不用 emoji**，不用重阴影，不用玻璃拟态
- 只动有问题的样式/结构，不要大范围重构没坏的代码
- 修改集中在 styles.css（首选）+ 必要时改 App.tsx / 组件的 className 或 JSX

## 验证

1. `cd frontend && npm run build` 必须通过，无 TS 错误
2. 输出改动清单：改了哪些页面/组件、每个问题怎么修的
3. 如果你发现某个问题无法从代码层面确定（需要视觉确认），单独列出来标注「需视觉确认」

## 输出

回复里写：
1. 发现的问题清单（按页面分组，标注 Critical/High/Medium）
2. 每个问题怎么修的
3. build 验证结果
4. 无法确认的问题（需浏览器视觉验证）
